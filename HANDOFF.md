goals, current status, next 3 tasks, known issues. Then commit it.


  4. Tomorrow, start the new agent with:

  python3 ask_brain.py "give me yesterday's project context, decisions, and next steps"
  python3 ask_brain.py --raw-only "current handoff context"